#pragma once

#include <string>

struct akas {
    std::string title_id;
    int ordering;
    std::string title;
    std::string region;
    std::string language;
    std::string types;
    std::string attributes;
    bool is_original_title;
};

